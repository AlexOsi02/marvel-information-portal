import spinner from './spinner.gif'

const Spinner = () => {
  return(
      <>
        <img style={{display: 'block', margin: "0 auto", marginTop: "93px" }} src={spinner} alt="spinner"/>
      </>
  )
}

export default Spinner;